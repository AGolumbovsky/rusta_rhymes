use std::mem;
// use sh;

fn main() {

    sh::stackAndHeap();

    /*const AAA:u8 = 333;
    // u unsigned
    // i integer
    let a:u8 = 123; // 8 bits
    println!("a is equal to {}", a);

    let mut b:i8 = 0; // mutable
    println!("b is: {}", b);
    b = 33;
    println!("b is: {}", b);

    let mut c = 578985389847;
    println!("c is of {}, size is {} bytes", c, mem::size_of_val(&c));

    let z:isize = 123; // isize/usize
    let size_of_z = mem::size_of_val(&z);
    println!("z is {}, it takes up {} bytes, {}-bit OS", z, size_of_z, size_of_z * 8);

    let mut poo = 1;
    poo += 1;
    println!("poo is {}", poo);

    let n = 5;
    let n_cubed = i32::pow(n, 3);
    println!("{} cubed is {}", n, n_cubed);

    // bitwise operators
    let c = 1 | 2;

    fn ass() {
        {
            println!("{}", AAA);
        }
    }*/
}